name: Spfx Build Pipeline

on:
  push:
    branches:
      - master
      - develop
  pr:
    branches:
      - develop
  workflow_dispatch:

  jobs:
    Build:
      runs-on: ubuntu-latest
      steps:
        - name: echo hello
          run: echo hello spfx 